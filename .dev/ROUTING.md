# Change routing

Start with the narrowest owning surface, then inspect the coupled callers and
state owners before editing. Paths are relative to the repository root.

| Touching | Inspect first | Coupled surfaces to inspect | Minimum targeted proof |
|---|---|---|---|
| Distribution governance, owned patches, or upstream history | `AGENTS.md`, `docs/lineage.md`, `docs/patch-discipline.md`, `docs/owned-patches.tsv` | `scripts/verify-lineage.sh`, `scripts/verify-patch-discipline.sh`, `docs/upstream-sync-rehearsal.md` | Lineage and patch verifiers; then the contract suite from a clean committed tree |
| App routes or components | `server/packages/happy-app/index.ts`, `server/packages/happy-app/sources/app/**`, `server/packages/happy-app/sources/components/**` | `server/packages/happy-app/sources/sync/**`, `server/packages/happy-app/sources/text/locales/{en,cn,de}.json`, UI inventory, changelog | App typecheck/tests, `i18n:check`, production web export |
| Claude↔Codex fresh-session continuation | `server/packages/happy-app/sources/hooks/useSessionQuickActions.ts`, `server/packages/happy-app/sources/components/{ProviderContinuationSheet,ProviderContinuationLinks}.tsx`, `server/packages/happy-app/sources/utils/providerContinuation.ts`, `server/packages/happy-app/sources/sync/{ops,storageTypes}.ts` | `server/packages/happy-cli/src/api/apiMachine.ts`, `server/packages/happy-cli/src/daemon/run.ts`, `server/packages/happy-cli/src/utils/createSessionMetadata.ts` | Prove a fresh opposite-provider spawn on the exact machine, path/worktree, and Commander; bounded recent visible text through the ordinary encrypted send; bidirectional links; and no provider-native resume with focused app/CLI tests plus rendered 1440×900 and 390×844 gestures. |
| File workspace consolidation | [`.dev/playbooks/file-workspaces.md`](playbooks/file-workspaces.md), `sources/-session/SessionView.tsx`, `sources/app/(app)/workspace/index.tsx` | `DesktopFileWorkspace`, `FileViewPanel`, `WorkspaceLinkViewer`, `WorkspaceFeedbackComposer`, `sync/ops.ts`, `utils/markdownWorkspaceLink.ts`, `components/MainView.tsx` | Follow the playbook's rendered desktop/mobile gesture matrix; then app typecheck/tests, UI inventory, `i18n:check`, and production web export |
| Active-session composer voice dictation | `server/packages/happy-app/sources/{-session/SessionView.tsx,hooks/useVoiceDictation.ts,components/{AgentInput,MultiTextInput}.tsx,sync/apiVoice.ts}` | `useVoiceInputAvailability`, `useDraft`, `POST /v1/voice/transcriptions`, and the separate realtime voice lifecycle | Prove availability gating, recording/transcribing/cancel/error/retry states, existing-draft append without sending, `/v1/voice/transcriptions` transport, and no composer call to `/v1/voice/conversations`; then run app typecheck/tests, `i18n:check`, and production web export |
| Provider registry, Agent Defaults, or new-session launch modes | `server/packages/happy-app/sources/utils/harnessCatalog.ts`, `server/packages/happy-app/sources/sync/agentDefaults.ts`, `server/packages/happy-app/sources/app/(app)/settings/agents.tsx` | Agent Defaults' explicit exact-machine capability-source selector and localized unavailable states, `components/modelModeOptions.ts`, `sync/rigSessionCreation.ts`, `hooks/useNewSessionDraft.ts`, full New Session, `HomeDock.tsx`, `useStartSessionFromDraft.ts`, settings persistence, and outbound mode metadata | Registry parity, non-empty provider groups, selector/unavailable rendering, exact-machine capability and late-revalidation launch tests, app typecheck/tests, UI inventory, `i18n:check`, and production web export |
| New provider or provider protocol behavior | [`.dev/playbooks/provider-onboarding.md`](playbooks/provider-onboarding.md); `server/packages/happy-wire/src/{messageMeta,sessionProtocol}.ts`; `server/packages/happy-cli/src/api/{types,apiSession}.ts`; daemon spawn and the provider adapter; for ACP, `src/agent/acp/{AcpBackend,sessionUpdateHandlers,AcpSessionManager,acpAgentConfig}.ts` | Capability discovery and exact-daemon validation; provider permission handler/policy; backend-neutral `AgentMessage`; app `sync/{typesMessageMeta,typesRaw,reducer/**}.ts`; `components/tools/{knownTools,ToolView}.tsx`; `utils/toolDisplay.ts`; attachment, resume, and fork policy | Open shared-metadata transit plus provider-boundary validation; exact launch arguments; permission callback behavior; raw/spec-shaped tool start/update/result/error correlation; meaningful generic rendering; package tests and a live provider smoke |
| Named credential connection, quota ownership, or reactive account rotation | `server/packages/happy-cli/src/commands/connect.ts`; `server/packages/happy-cli/src/credentialPool/{store,providerLimits,providerLimitNotice,rotation}.ts`; `server/packages/happy-cli/src/claude/{runClaude,claudeRemote,claudeRemoteLauncher}.ts`; `server/packages/happy-cli/src/claude/utils/usageLimits.ts` | Daemon rotation/resume in `src/daemon/run.ts`; encrypted event persistence in `src/api/{api,apiSession}.ts`; app `sources/sync/{storageTypes,typesRaw,reducer/reducer}.ts`; `sources/components/MessageView.tsx`; en/cn/de catalogs; CLI credential-pool README | Focus account parsing, Claude setup-token output, quota merge/classification, resume initialization, rotation ordering, API persistence, app schema/reducer/rendering, and locale tests; then run full Happy CLI/app checks and `i18n:check` |
| Localized copy, catalog keys, or UI ownership | `server/packages/happy-app/sources/text/locales/{en,cn,de}.json`, `server/packages/happy-app/scripts/validate-i18n.mjs`, `server/packages/happy-app/scripts/generate-ui-surface-inventory.mjs` | Owning route/component, generated `server/packages/happy-app/sources/text/ui-surface-inventory.json` and `server/packages/happy-app/sources/text/ui-tree.html`, changelog | Generate when required, review the diff, then `i18n:check` |
| Sync, message ordering, reconnect, or session visibility | `server/packages/happy-app/sources/sync/{sync,storage,apiSocket,apiTypes}.ts` | `happy-wire`, server API/socket/event routing, CLI `apiSession` | Focused tests plus app, wire, CLI, and server suites |
| Shared protocol or encrypted payload shape | `server/packages/happy-wire/src/index.ts` and the relevant schema module | Every importer under app, CLI, server, and `happy-agent` | Wire tests and all affected consumer typechecks/tests |
| CLI command or existing provider behavior | `server/packages/happy-cli/bin/happy.mjs`, `server/packages/happy-cli/src/index.ts`, provider directory under `server/packages/happy-cli/src/`; GrokBuild uses `src/agent/acp/{acpAgentConfig,runAcp,sessionConfigMetadata}.ts` | `server/packages/happy-cli/src/{capabilities/agentCapabilities,utils/detectCLI}.ts`, daemon, API clients, and app agent selection, attachment, resume, and fork policy; use the provider-onboarding row when the provider contract or protocol changes | `pnpm --filter happy typecheck` and `pnpm --filter happy test`; for Grok also cover ACP config/capabilities, permission callbacks, raw ACP tool events, and affected app tests |
| Codex turn lifecycle | `server/packages/happy-cli/src/codex/{runCodex,codexAppServerClient,codexTurnRouting}.ts` | Colocated tests, `server/packages/happy-cli/src/api/apiSession.ts`, queue and protocol mapping | Codex-focused tests, then the Happy CLI package test |
| Daemon or machine RPC | `server/packages/happy-cli/src/daemon/run.ts`, `server/packages/happy-cli/src/api/apiMachine.ts`, `server/packages/happy-cli/src/modules/common/registerCommonHandlers.ts` | `server/packages/happy-app/sources/sync/ops.ts`, `server/packages/happy-server/sources/app/api/socket/rpcHandler.ts`, `server/packages/happy-agent/src/machineRpc.ts` | Focused daemon/API tests and the cross-package contract suite |
| Commander or AgentContext behavior | `server/packages/happy-cli/src/agentContext/commanderContext.ts`, `server/packages/happy-cli/src/commands/commander.ts` | `server/packages/happy-wire/src/commanderContext.ts`, app session creation/selector, automation bootstrap | Commander/context tests plus affected CLI/app tests |
| Automations | `server/packages/happy-wire/src/automation.ts`; `server/packages/happy-cli/src/automations/{store,service,sessionBootstrap,providerOutcome,unattendedPolicy}.ts`; session command owner `server/packages/happy-app/sources/utils/heartbeatCommand.ts` | Daemon lifecycle/API handlers, `apiSession`, `MessageQueue2`, Claude/Codex queue consumers, and app Session Info/SessionView/automation route/card/ops | Wire, CLI, and app automation tests; run the [unattended provider smoke](playbooks/automation-unattended-smoke.md) for every automation-capable provider; heartbeat coverage must include exact-session resume, FIFO isolation, coalescing, and lifecycle receipts |
| Server API, events, or database | `server/packages/happy-server/sources/{main,index,standalone}.ts`, `server/packages/happy-server/sources/app/api/api.ts`, `server/packages/happy-server/prisma/schema.prisma` | Route handler, event router, app/CLI caller, wire schema, storage adapter | Server typecheck/test/build plus affected client tests |
| Self-host server bundle | `server/packages/happy-server-self-host/scripts/build-runtime.cjs`, `server/Dockerfile` | Happy server dependency list, Prisma migrations, app web bundle | Self-host build and `scripts/test-component-deployment-contract.sh` |
| Local installer or `happyherd` alias | `install.sh`, `installers/{uninstall,cleanup-legacy}.sh`, `server/packages/happyherd-cli/src/{cli,runtime}.ts` | Normal Happy settings/server/daemon, installer docs | Thin-wrapper tests and local-installer contract |
| Governed Discord agent | `server/packages/happyherd-agent/src/{index,config,bridge,store,happy,broker,httpServer,manifest}.ts` | `server/packages/happy-agent/src/control.ts`, deploy templates, runtime boundary | Both agent suites and runtime/sandbox contracts |
| `happy-agent` remote control | `server/packages/happy-agent/src/{index,control,api,machineRpc,session}.ts` | Server APIs/socket and `happyherd-agent` | `happy-agent` tests; integration tests when prerequisites exist |
| Component deployment or local installer | Root `scripts/`, `deploy/`, `install.sh`, `installers/`, root `.github/workflows/` | `.dev/playbooks/post-update-restart.md`, component deployment, local installer, and runtime-boundary docs | Component or local-installer contract followed by the full suite; for a combined server/daemon activation, also retain the playbook's runtime read-backs |
| Codium desktop | `server/packages/codium/electron.vite.config.ts`, `server/packages/codium/sources/boot/**`, renderer routes/plugins | Agent workers and shared IPC protocols | Codium typecheck/test; no root required CI job currently owns it |
| App log receiver | `server/packages/happy-app-logs/src/server.ts`, `server/packages/happy-app/sources/utils/consoleLogging.ts` | App log-server setting and the configured log root | Manual local start/smoke; no repository test currently owns it |

## Routing rules

- Root `.github/workflows/**` is the active repository CI. Files under
  `server/.github/workflows/**` are retained upstream context.
- `server/` preserves upstream history, but HappyHerd-owned changes inside it
  still follow root patch discipline and root verification.
- Shared runtime method strings may be contracts even when they are not all
  declared in `happy-wire`; trace both endpoints before renaming one.
- Do not normalize retained upstream product names or URLs merely because they
  differ from distribution-owned names. `AGENTS.md` defines the branding scope.
- Upstream-sync rehearsals retain their own provenance rules. Self-host server,
  CLI/daemon, local installer, mobile, and governed-agent
  deployment remain independent of those release channels.
