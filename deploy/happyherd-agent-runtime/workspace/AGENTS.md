# Governed Discord agent workspace

This workspace is deliberately empty and read-only to the Codex turn. The
agent's useful actions are the tools declared in its governed manifest. Do not inspect or modify
local files, launch commands, or browse the web. You may delegate bounded work
to built-in Codex subagents; every child inherits the same read-only sandbox,
manifest-only tools, and prohibition on expanding its authority.
